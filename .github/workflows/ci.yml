name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Run static smoke checks
        run: ./scripts/smoke-test.sh --static-only

  live-smoke:
    if: ${{ github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    env:
      TAMUS_API_KEY: ${{ secrets.TAMUS_API_KEY }}
    steps:
      - name: Check out
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Install Codex CLI
        run: npm install -g @openai/codex

      - name: Install overlay
        run: |
          export PATH="$HOME/bin:$PATH"
          ./install.sh

      - name: Require TAMUS_API_KEY
        run: |
          if [[ -z "${TAMUS_API_KEY:-}" ]]; then
            echo "Repository secret TAMUS_API_KEY is not set."
            exit 1
          fi

      - name: Run live smoke test
        run: |
          export PATH="$HOME/bin:$PATH"
          ./scripts/smoke-test.sh
